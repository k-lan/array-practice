/**
 * A simple class that calculates the average of an int array[].
 * */
public class avg
{
    public static void main(String[] args) //test the method
    {
        int[] x = new int[]{1,4};
        System.out.println(average(x));
    }

    public static double average(int[] array) //the actual calculation.
    {
        double i = 0;
        for(int x : array) {
               i += x;
        }
        if(i != 0) {
            i = i/array.length;   
        }
        return i;
    }
}
